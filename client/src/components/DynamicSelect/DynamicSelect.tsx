type DynamicSelectProps = {
  range: number;
  startAt?: number;
  selectValue: string;
  selectName: string;
  header?: string;
  set: Dispatch<SetStateAction<string>>;
};

const DynamicSelect = () => {};

export default DynamicSelect;
